let myBook={
    title: '1984',
    author: 'George Orwell',
    pageCount: 326
}

console.log(`${myBook.title} is written by ${myBook.author} and contains ${myBook.pageCount} pages`)

let myInfo={
    name: 'Manthan Gupta',
    age:21,
    location: 'Greater Noida'
}

console.log(`${myInfo.name} is ${myInfo.age} years old and lives in ${myInfo.location}`)

myInfo.age+=1

console.log(`${myInfo.name} is ${myInfo.age} years old and lives in ${myInfo.location}`)